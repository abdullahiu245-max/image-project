# Copy this to .env.local for local development, and set the same
# variable in Vercel's project settings (Settings -> Environment
# Variables) for production, pointing at your deployed backend URL.

NEXT_PUBLIC_API_URL=http://localhost:8080
